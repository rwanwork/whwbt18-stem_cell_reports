#!/usr/bin/perl
use warnings;
use strict;
use diagnostics;

printf "Sample";
printf "\tMapping rate";

printf "\tSingle/Paired-end";

printf "\tNumber of reads (1, millions)";
printf "\tMinimum read length (1)";
printf "\tMaximum read length (1)";
printf "\tAverage read length (1)";
printf "\tNumber of bases (1, billions)";

printf "\tNumber of reads (2, millions)";
printf "\tMinimum read length (2)";
printf "\tMaximum read length (2)";
printf "\tAverage read length (2)";
printf "\tNumber of bases (2, billions)";

printf "\n";


